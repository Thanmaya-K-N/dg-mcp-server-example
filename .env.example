<<<<<<< HEAD
# Copy to .env and fill in. Used when you run the MCP server yourself (e.g. npm start).
=======
# Copy to .env and fill in. Used when you run the MCP server yourself (e.g. python main.py).
>>>>>>> 6828d3c (Replace TypeScript with Python (FastMCP, Pydantic v2))
# Cursor does NOT inject mcp.json "env" when using type "http" (it only connects to the URL).

# Required for Gateway auth (list datasets, get schema, query, etc.)
DATAGROOM_PAT_TOKEN=dgpat_your_token_here

# Gateway base URL (default if unset)
DATAGROOM_GATEWAY_URL=http://localhost:8887

# MongoDB (optional; server starts without it and tools use Gateway)
# MONGODB_URL=mongodb://localhost:27017

# MCP server port
# MCP_SERVER_PORT=3000
